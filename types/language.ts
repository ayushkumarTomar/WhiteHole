type footerItem = {
    id: string;
    title: string;
    action: string;
} 

export type languageSongs = {
    playlist: footerItem[];
    artist: footerItem[];
    album: footerItem[]
    actor: footerItem[]

}

